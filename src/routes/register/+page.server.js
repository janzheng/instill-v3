
import { profileActions } from '$instill-helpers/instill-server'

// FUTURE: EDIT PROFILE
export const actions = {
  updateProfile: profileActions.updateProfile,
  register: profileActions.register,
  emailCheck: profileActions.emailCheck,
};

